<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zealsoft.helloapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloApp">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.HelloApp.NoActionBar">

        </activity>

        <activity android:name=".DemoActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".FirstActivity">

        </activity>

        <activity android:name=".ColorActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".ShowActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>
        <activity android:name=".BottomNavActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".MyTabActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".DialogActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".ShapeActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".LoginActivity"
            android:theme="@style/Theme.HelloApp"
            android:label="Login">

        </activity>
        <activity android:name=".HomeActivity"
            android:theme="@style/Theme.HelloApp"
            android:label="Wlecome">

        </activity>

        <activity android:name=".SplashActivity"
            android:theme="@style/Theme.HelloApp.NoActionBar">

        </activity>

        <activity android:name=".NotiActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".CIActivity"
            android:theme="@style/Theme.HelloApp"
            android:label="Circle Image View">

        </activity>

        <activity android:name=".StudentFormActivity"
            android:theme="@style/Theme.HelloApp"
            android:label="Form">

        </activity>

        <activity android:name=".StudentListActivity"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".StudentUpdateForm"
            android:theme="@style/Theme.HelloApp">

        </activity>

        <activity android:name=".AuthWorks.RegistrationActivity"
            android:theme="@style/Theme.HelloApp"
            android:label="Registration">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AuthWorks.LoginActivity"
            android:theme="@style/Theme.HelloApp"
            android:label="Login">

        </activity>

    </application>

</manifest>